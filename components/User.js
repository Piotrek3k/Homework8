class User {
    // class that represents a Book object. It has following properties:

    // name - string
    // email - string
    // userId - number

    constructor(name, email, userId) {
      this.name = name;
      this.email = email;
      this.userId = userId;
    }
  }
  
  module.exports = User;